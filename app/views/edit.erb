<div class="container">
  <form action="/skills/<%= @skill.id %>" method="POST">
    <h3>Edit skill:</h3>
    <input type="hidden" name="_method" value="PUT" />
    <input type='text' name='skill[title]' value="<%= @skill.title %>" /><br/>
    <input type='submit' class='btn btn-default'/>
  </form>
</div>
